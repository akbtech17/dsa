// buyAndSellStockProblem.cpp
#include <iostream>
#include <climits>
using namespace std;

int main() {
	int n;
	cin >> n;
	int arr[n] = {0};
	for (int i = 0 ; i < n; i++) cin >> arr[i];

	// we need to find pair with max on right side and whose diff is max


	cout << "buy the stock when price is " << by << " and sell it when price is " << sl;
	cout << " to get a profit of " << sl - by;

	return 0;
}